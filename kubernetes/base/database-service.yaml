apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: farmdirect
spec:
  selector:
    app: database
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP